language: clojure
jdk:
  - oraclejdk8

before_deploy:
  - lein jar
deploy:
  provider: releases
  api_key: "${GH_TOKEN}"
  file_glob: true
  file: $TRAVIS_BUILD_DIR/target/*.jar
  skip_cleanup: true
  overwrite: true
  on:
    tags: true
after_deploy:
  - lein deploy releases